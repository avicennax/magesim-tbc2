#CC specifies which compiler we're using
CC = g++ -std=c++11

#OBJ_NAME specifies the name of our exectuable
OBJ_NAME = magesim

#This is the target that compiles our executable
main: src/main.cpp
	$(CC) $? $(COMPILER_FLAGS) $(LINKER_FLAGS) -o $(OBJ_NAME)

clean:
	rm -rf build

# Compile all headers for RL
# compile: clean
# 	@mkdir -p build/sim
# 	$(CC) -fPIC -c src/rl/interface.cpp -o build/sim/interface.o -I src/sim

cython: clean
	python setup.py install

segfault:
	python test/init_python_sim.py